apiVersion: v1
kind: Service
metadata:
  name: airline-satisfaction-svc
spec:
  type: LoadBalancer
  selector:
    app: airline-satisfaction
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000